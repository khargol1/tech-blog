    <article class="post">
      <div class="title">
        <h2><a href="/post/{{id}}">{{title}}</a></h2>
        <p>by {{user.username}} {{format_date created_at}}</p>
        <span>{{post_content}}</span>
      </div>
      <div class="meta">
        UpVotes:{{vote_count}}        
        <h5>Comments: {{comments.length}}</h5>
      </div>
    </article>